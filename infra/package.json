{
  "name": "@eventmgr/infra",
  "version": "0.1.0",
  "private": true,
  "bin": { "eventmgr": "bin/eventmgr.js" },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "synth": "cdk synth",
    "deploy": "cdk deploy",
    "test": "node --test --experimental-strip-types 'test/**/*.test.ts'"
  },
  "dependencies": {
    "aws-cdk-lib": "^2.160.0",
    "constructs": "^10.3.0"
  },
  "devDependencies": {
    "aws-cdk": "^2.160.0",
    "@types/node": "^22.0.0",
    "esbuild": "^0.23.0",
    "source-map-support": "^0.5.21",
    "ts-node": "^10.9.2",
    "typescript": "^5.6.0"
  }
}
