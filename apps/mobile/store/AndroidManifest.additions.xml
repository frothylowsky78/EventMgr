<!--
  APPLIED — these entries are already merged into android/app/src/main/AndroidManifest.xml.
  Kept as the reference for what was added and why; re-apply only if the Android project is
  regenerated.

  - <uses-permission> goes as a direct child of <manifest>.
  - The intents below were merged INTO the existing <queries> block rather than added as a
    second one — the manifest schema allows only one <queries> element per file, and the
    generated PROCESS_TEXT intent must be preserved.
  - Set android:label on <application> (or via a string resource).

  POST_NOTIFICATIONS is deliberately absent: push notifications are cut from v1. Do not add it.
-->

<!-- Direct children of <manifest> -->
<uses-permission android:name="android.permission.INTERNET"/>
<uses-permission android:name="android.permission.CAMERA"/>
<!-- image_picker: reading images. Android 13+ uses READ_MEDIA_IMAGES; older uses READ_EXTERNAL_STORAGE. -->
<uses-permission android:name="android.permission.READ_MEDIA_IMAGES"/>
<uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
    android:maxSdkVersion="32"/>

<!-- url_launcher needs explicit intent visibility on Android 11+ (package visibility). -->
<queries>
  <intent>
    <action android:name="android.intent.action.VIEW"/>
    <data android:scheme="https"/>
  </intent>
  <intent>
    <action android:name="android.intent.action.DIAL"/>
    <data android:scheme="tel"/>
  </intent>
  <intent>
    <action android:name="android.intent.action.SENDTO"/>
    <data android:scheme="mailto"/>
  </intent>
  <intent>
    <action android:name="android.intent.action.VIEW"/>
    <data android:scheme="geo"/>
  </intent>
</queries>

<!-- On <application>: android:label="Event Companion"  (and keep android:usesCleartextTraffic="false") -->
