<!--
  Merge into android/app/src/main/AndroidManifest.xml after `flutter create .`.
  - <uses-permission> and <queries> go as direct children of <manifest>.
  - Set android:label on <application> (or via a string resource).
-->

<!-- Direct children of <manifest> -->
<uses-permission android:name="android.permission.INTERNET"/>
<uses-permission android:name="android.permission.CAMERA"/>
<!-- image_picker: reading images. Android 13+ uses READ_MEDIA_IMAGES; older uses READ_EXTERNAL_STORAGE. -->
<uses-permission android:name="android.permission.READ_MEDIA_IMAGES"/>
<uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
    android:maxSdkVersion="32"/>
<uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>

<!-- url_launcher needs explicit intent visibility on Android 11+ (package visibility). -->
<queries>
  <intent>
    <action android:name="android.intent.action.VIEW"/>
    <data android:scheme="https"/>
  </intent>
  <intent>
    <action android:name="android.intent.action.DIAL"/>
    <data android:scheme="tel"/>
  </intent>
  <intent>
    <action android:name="android.intent.action.SENDTO"/>
    <data android:scheme="mailto"/>
  </intent>
  <intent>
    <action android:name="android.intent.action.VIEW"/>
    <data android:scheme="geo"/>
  </intent>
</queries>

<!-- On <application>: android:label="VIP Summit"  (and keep android:usesCleartextTraffic="false") -->
